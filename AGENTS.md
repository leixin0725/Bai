# 项目宪章（AGENTS.md）

> 本文件由原项目宪章（v1.1.0）提取整理而来，作为 Bai Agent 的工程约束总纲。
> **Version**: 1.0.1 | **提取日期**: 2026-08-11 | **修订日期**: 2026-08-11 | 后续修改请同步更新版本号与修订日期。

## 核心原则

### I. 清晰、可扩展、可维护（最高优先级）
生产代码、测试和配置 MUST 具有清晰的职责、命名、控制流和边界；变更 MUST 让维护者能够在不依赖隐含上下文的情况下理解其目的与影响。设计 MUST 支持已知需求的合理扩展，并避免让局部变更引发无关模块的大范围修改。评审发现职责混杂、重复业务规则或难以安全修改的结构时，相关问题 MUST 在合入前修复，或在计划的复杂度跟踪中记录原因、风险和后续处理条件。本原则优先于其后的解耦与简单性偏好。

### II. 模块解耦与可读性
在满足原则 I 的前提下，模块 MUST 保持单一且可说明的职责，通过明确、最小且稳定的契约协作；依赖方向 MUST 可追踪，并 MUST 避免循环依赖、共享可变状态和跨层泄漏实现细节。能够提升独立测试、替换或复用能力的关注点 SHOULD 被分离；若额外抽象反而降低清晰性或可维护性，则 MUST 以原则 I 为准，不得为假设性的未来需求增加层级。代码结构和命名 MUST 让调用关系与数据流可直接阅读。

### III. 简单且易懂的实现
在满足原则 I 和 II 的前提下，实现 MUST 选择能够正确满足当前需求的最小方案，优先使用团队可直接理解的语言特性、控制流和依赖。不得仅为展示技巧、未经证实的性能收益或可能永远不会出现的需求引入框架、模式、服务或通用化层。任何显著增加组件、间接层或运行时依赖的方案 MUST 在计划中说明必要性，并记录被否决的更简单方案及其不足。

### IV. 简体中文与可追溯注释
代码注释 MUST 使用简体中文（zh-CN）。新增注释时 MUST 一次性写清目的、约束、边界条件和不直观决策的原因，不得只复述语法。模块入口、核心业务规则、公共契约以及非显而易见的安全约束 MUST 有清晰注释。已经写好的注释 MUST NOT 被随意改写或删除；只有在注释已经过时、错误或具有误导性时才可更新，并 MUST 在新增或修改的注释中加入 `[YYYY-MM-DD]` 时间戳或 `[vX.Y.Z]` 版本标记。评审 MUST 同时检查注释与代码一致，避免保留失真的说明。

### V. Git 仓库与重大修改提交
项目 MUST 使用 Git 维护版本历史。完成并验证重大修改后 MUST 立即创建一个边界清晰的提交；重大修改包括核心业务逻辑、公共契约、架构或模块边界、数据结构、安全控制、关键依赖或平台迁移，以及跨模块行为的实质变化。提交 MUST 只包含同一目的的相关文件，提交信息 MUST 说明意图，且不得夹带敏感凭据或无关工作区改动。提交前 MUST 执行适用的测试和质量检查；无法通过时不得以“稍后修复”为由把重大修改标记为完成。

### VI. 核心业务逻辑测试（不可妥协）
每条核心业务规则及其行为变更 MUST 由自动化测试覆盖。测试 MUST 至少覆盖主要成功路径、相关边界条件和关键失败路径；缺陷修复 MUST 增加能够复现原问题的回归测试。规格和计划 MUST 标识核心业务逻辑及其可验证结果，任务清单 MUST 在对应实现任务之前或同时包含测试任务。适用测试在重大提交、合并和发布前 MUST 全部通过；任何例外都必须先通过宪章修订，而不得在单个功能中临时豁免。

### VII. 敏感凭据保护（不可妥协）
密码、令牌、私钥、连接密钥及其他敏感凭据 MUST NOT 出现在版本库、源码、注释、日志、测试夹具、示例值或生成制品中。运行时凭据 MUST 通过受控环境变量、密钥管理系统或等效安全机制注入，并遵循最小权限、按环境隔离、可轮换和输出脱敏要求。示例配置 MUST 仅使用明确的无效占位符；计划和评审 MUST 识别凭据流向及泄露风险。发现疑似泄露时 MUST 停止传播、撤销或轮换受影响凭据、记录处置结果，并在继续交付前完成仓库与日志检查。

### VIII. 重大更新与文档同步（不可妥协）
每次重大更新 MUST 在同一交付里评估并同步维护所有受影响的项目文档；重大更新的范围采用原则 V 的定义。受影响文档包括但不限于 README、quickstart、运行手册、配置说明、公共接口与存储契约，以及对应功能的规格、计划和任务。文档 MUST 准确反映更新后的行为、命令、配置、兼容性、迁移或恢复步骤和验证方法中适用的内容，且示例 MUST 可执行或明确标注为示意。若评估后无需修改文档，计划或评审记录 MUST 写明 `N/A` 及理由；不得以“代码已经清晰”替代文档影响评估。受影响文档未完成更新和适用验证时，重大更新 MUST NOT 被视为完成，也不得按原则 V 创建完成提交；文档变更 MUST 与对应重大更新位于同一原子提交中。

## 工程优先级

当设计目标发生冲突时，决策顺序 MUST 为：原则 I（清晰、可扩展、可维护）优先于原则 II（解耦与可读性），原则 II 优先于原则 III（简单实现）。原则 IV 至 VIII 是独立的强制质量门禁，不得通过上述排序予以削弱。所有复杂度例外 MUST 在实现前写入计划的复杂度跟踪，包含具体收益、风险、被否决的简单方案和复核条件。

## 开发工作流与质量门禁

1. 规格阶段 MUST 标识核心业务规则、可测验收结果、功能是否接触敏感凭据，以及重大更新预期影响的项目文档；规格不得包含真实凭据。
2. 计划阶段 MUST 说明模块职责与依赖、最简单可行方案、核心测试策略、注释影响和凭据保护方案；宪章检查失败时不得开始实现；重大更新还 MUST 列出受影响文档、所需内容和验证方式，或记录 `N/A` 及理由。
3. 任务阶段 MUST 为每项核心业务逻辑安排自动化测试，并为适用的凭据保护、注释更新、文档同步和质量验证安排具体工作；重大更新的文档任务 MUST 位于对应实现阶段且先于其完成检查点，不得仅推迟到最终润色阶段。
4. 实现与评审阶段 MUST 按原则 I、II、III 的顺序评估设计，并校验 zh-CN 注释、测试覆盖、凭据安全和文档同步。
5. 完成重大修改后 MUST 验证代码和受影响文档的一致性及适用命令、链接和示例，再按原则 V 和 VIII 创建包含对应文档的原子提交；提交哈希或提交信息作为阶段完成证据。

主分支的历史规格、计划与任务制品已归档至 `docs/archived/specs/`，不再初始化规格流程；需要规格/计划/任务工作流时，在独立分支或 worktree 中按本宪章执行。

## 治理

本宪章是项目工程决策的最高约束；规格、计划、任务、代码评审和运行指南若与其冲突，MUST 以本宪章为准。修订提案 MUST 说明变更内容、理由、受影响制品、迁移或补救计划，并由项目维护者或其明确授权的评审者批准。

宪章版本遵循语义化版本：删除原则、降低强制级别或对既有治理作不兼容重定义时提升 MAJOR；新增原则、章节或实质扩展治理要求时提升 MINOR；不改变含义的澄清、措辞和错字修正提升 PATCH。每次修订 MUST 更新版本号和修订日期，并同步检查所有受影响的项目文档与规范；违反 MUST 条款的工作不得合并或发布，若条款本身需要改变，必须先完成宪章修订，不得通过功能级例外绕过。
